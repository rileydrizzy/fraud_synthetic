# Pinned environment for synthetic_fraud_validation.ipynb
#
# These versions are the ones the recorded results in outputs/validation_metrics.json
# were produced with. The notebook is bit-for-bit reproducible ONLY on these versions
# (see README.md). Loosening any pin may change the numbers in the report.
#
# Install with setup_env.sh, not by hand.

# --- Core numerics / data ---
numpy==2.5.1
pandas==2.3.3
scipy==1.18.0

# --- Modelling ---
scikit-learn==1.9.0
torch==2.13.0
ctgan==0.12.1
rdt==1.21.1
tqdm==4.70.0

# --- Plotting ---
matplotlib==3.11.1
seaborn==0.13.2
pillow==12.3.0

# --- Notebook execution ---
notebook==7.6.1
ipykernel==6.26.0
nbformat==5.10.4
nbconvert==7.17.1
